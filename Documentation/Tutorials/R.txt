# R scripts notes (draft)

1. The variables that are passed to R fuctions are not allowed to have dots in their names.

2. The variables' values from input_settings (xml) are being passed as strings. The convertion i.e. from string to integer have to be dane inside R script.
